# TODO - Dark/Light Mode Toggle

- [x] Update `index.html` to add theme toggle button UI.
- [ ] Update `index.html` inline JS to initialize theme (localStorage + prefers-color-scheme) and toggle on click.
- [ ] Update `index.html` inline JS to initialize theme (localStorage + prefers-color-scheme) and toggle on click.
- [ ] Update `style.css` to support light theme via `html[data-theme="light"]` variable overrides.
- [ ] Replace remaining hardcoded dark rgba backgrounds with variable-based equivalents where needed.
- [ ] Manual test: toggle works, persists on refresh, mobile menu unaffected.
